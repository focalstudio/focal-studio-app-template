import type { CapacitorConfig } from '@capacitor/cli';

// After cloning this template, replace [APP_ID] and [APP_NAME] with your app's values.
// Then run: npx cap add android   (and optionally: npx cap add ios)
const config: CapacitorConfig = {
  appId: '[APP_ID]',
  appName: '[APP_NAME]',
  webDir: 'dist',
};

export default config;
