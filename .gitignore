# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
build/
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# PWA dev build output (auto-generated, not source)
dev-dist/

# Build artefacts and OS files that should never be committed
*.apk
*.aab
*.webloc

# Claude Code local files (skills, memory, settings — personal, not shared)
.claude/skills/
.claude/settings.local.json
IMPROVEMENTS.txt

# iOS build artifacts (generated by cap sync — not source)
ios/App/App/public/
ios/App/App/config.xml
ios/capacitor-cordova-ios-plugins/

# Android signing secrets — NEVER commit
android/*.jks
android/*.keystore
android/keystore.properties

# R / RStudio artifacts
.Rhistory
.RData
