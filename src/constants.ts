// App identity — these values are filled by the SETUP.md placeholder replacement step.
// After setup, only this file needs to be updated if identity values change.
export const APP_NAME = "[APP_NAME]";
export const APP_SLUG = "[APP_SLUG]";
export const APP_ID = "[APP_ID]";
export const APP_COLOR = "[APP_COLOR]";
export const APP_COLOR_DARK = "[APP_COLOR_DARK]";

// Storage key prefix — matches APP_SLUG, used to namespace all localStorage keys.
export const STORAGE_PREFIX = "[APP_SLUG]";
