# CLAUDE.md

Project-specific Claude Code instructions live in [`.claude/CLAUDE.md`](.claude/CLAUDE.md).

This root file is a pointer. The `.claude/` directory is where Claude Code automatically looks for project instructions when running from this repo root.
